# Surf Fishing Window v0.2 — Map Update

Replace these four files in the GitHub repository:

- index.html
- styles.css
- app.js
- service-worker.js

Keep `manifest.webmanifest` and the `icons` folder unchanged.

For each file on iPhone: open it, tap the pencil, replace all text, and commit directly to `main`.

After all four commits, wait 1–2 minutes and reopen the live site. If the installed Home Screen copy stays old, close it fully and reopen it. If necessary, remove and reinstall the Home Screen app.
